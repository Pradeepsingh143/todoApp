// **** /////   Api Routes   ///// ****

// User Routes

// 1. POST (/api/createUser)   (// name, email, appwriteId = req.body)
// 2. GET (/api/todos)   (// userId = req.query)

// todo Routes

// 1. Post (/api/todoCreate) (title, task, userId = req.body)